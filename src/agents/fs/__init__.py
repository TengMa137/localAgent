"""Filesystem specialist package.

Files:
- `contracts.py`: typed path-analysis and filesystem-agent result models.
- `path_policy.py`: deterministic path-hint validation and replacement discovery.
- `prompts.py`: filesystem system prompt and rendered per-task context.
- `toolset.py`: task scope, write approval, and duplicate-read guards.

`agents.fs_agent` is the compatibility facade and execution coordinator.
"""
